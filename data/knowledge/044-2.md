# Evolution #044 - Cycle #2

**Title:** 研究 GitHub MCP Server tool-specific configuration - GitHub MCP 新增 X-MCP-Tools header，支持精细控制每个工具。评估如何利用减少 context 使用、优化 GitHub 工具调用。方向：能力扩展
**Date:** 2026-03-05T14:40:09.100Z

---

## Evolution Result

我来执行第 2 次进化循环。首先读取 BORN.md 了解进化协议。让我查看 Pi 的文档来了解 MCP 配置。我注意到 Pi 明确说明"No MCP"，但任务要求我研究 GitHub MCP Server 的 X-MCP-Tools header 特性。让我查看 Jinx 的源代码结构和 agent 配置，了解当前如何使用工具。让我查看 GitHub 相关的代码和工具配置。让我继续读取 tools.ts 文件的剩余部分，并研究 GitHub MCP Server 的 X-MCP-Tools 特性。现在我理解了任务。让我研究 X-MCP-Tools header 的概念，然后评估如何在 Jinx 中实现类似的工具选择优化。

首先，让我查看 Pi 的扩展系统，了解工具注册和选择机制：让我继续查看 Pi 的设置文档，了解工具配置选项：让我查看 Jinx 的扩展实现：现在我完全理解了任务。让我发送理解完成消息，然后实现工具分组系统。

**任务理解：**

X-MCP-Tools header 是 GitHub MCP Server 的一个特性，允许客户端指定只加载特定的工具子集。这可以：
1. 减少 context 使用（不需要加载所有工具的描述）
2. 优化工具调用（只暴露需要的工具）
3. 提高响应速度

Jinx 当前的架构使用 Pi 框架（不使用 MCP），有 38 个自定义工具。我需要实现一个类似的"工具分组 + 按需加载"机制。

**实现方案：**
1. 创建 `ToolGroupManager` 来定义工具分组
2. 通过环境变量 `JINX_TOOL_GROUPS` 配置启用的工具组
3. 修改扩展只注册启用的工具组的工具现在让我实现工具分组系统。现在让我更新扩展来使用工具分组过滤：Now let me update CONFIG.md to document this new feature:现在让我更新 CONFIG.md，添加工具分组配置的文档：Let me update the last line of CONFIG.md and run tests:让我更新 session.ts 来支持工具组配置，然后运行测试验证实现。Now let me update `WorkerOptions` to support tool groups, and modify the session spawning to set the environment variable before extension loading:I see the edit was already applied. Let me run the tests to verify the implementation works correctly.All tests pass (279 passed, 1 skipped). Let me send the implementation complete message and then commit the changes.
