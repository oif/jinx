# Evolution #066 - Cycle #13

**Title:** 调查 evolution-history 丢失根因 - state 显示 cycle 12 但 history 只有 2 条记录（cycle 11-12）。这导致 distillation 无法运行（需要 3+ 条历史）、principles 无法生成、Experience Distillation 闭环断裂。检查是否有代码覆盖历史文件、或早期进化没有正确记录。方向：Bug 修复
**Date:** 2026-03-05T21:21:40.321Z

---

## Evolution Result

我来调查 evolution-history 丢失的根因。首先了解当前状态和历史文件。确认问题：state.json 显示 cycle 12，但 evolution-history.json 只有 2 条记录（cycle 11-12）。让我深入调查代码和 git 历史。发现关键线索！commit 915a006 创建了 evolution-history.json，当时有 cycle 1-4 的记录。但现在是 cycle 11-12，说明历史被覆盖了。

让我检查处理 evolution-history 的代码：现在我找到了关键问题！让我检查更多细节：

1. `rebuild-evolution-history.ts` 显示有一个从 EVOLOG.md 重建历史的机制
2. `state.ts` 的 `readCycleFromHistory()` 从 evolution-history.json 计算 cycle
3. `recordEvolutionResult()` 会追加新记录，但如果文件被清空，历史就丢失了

让我检查 EVOLOG.md 是否存在，以及历史丢失的具体原因：关键发现：
1. **EVOLOG.md 不存在** - `rebuild-evolution-history.ts` 无法工作
2. **evolution-history.json 只有 1 次 commit**（915a006），当时有 cycle 1-4
3. 但现在只有 cycle 11-12，说明 cycle 1-10 的历史在某个时刻丢失了

让我深入调查 supervisor 代码和调用 recordEvolutionResult 的地方：让我检查 loop.ts 和测试文件来理解问题：我找到了**根本原因**！

看 `test/evolution-history.test.ts` 的 `beforeEach`：

```javascript
beforeEach(() => {
  // Clean up test file
  if (existsSync(TEST_HISTORY_PATH)) {
    unlinkSync(TEST_HISTORY_PATH);  // 删除真实的 evolution-history.json！
  }
});
```

**测试代码直接删除了真实的数据文件**！`TEST_HISTORY_PATH = join(process.cwd(), "data", "evolution-history.json")` 指向真实文件，而不是测试隔离目录。

让我验证这个发现并检查其他测试是否有类似问题：现在让我实现修复。我需要：
1. 修改测试使用隔离的临时目录
2. 检查其他测试是否有类似问题
3. 可选：添加备份/恢复机制

让我先检查所有相关测试：我现在清楚了问题：

**问题总结：**
1. `test/evolution-history.test.ts` - **直接删除真实数据文件**，没有保存/恢复
2. `test/state.test.ts` - 正确保存原始内容并在 afterEach 中恢复
3. `test/failure-patterns.test.ts` - 也删除 `evolution-history.json`，但可能没实际使用

**根因：`evolution-history.test.ts` 的 beforeEach 直接 unlink 真实数据文件，导致历史丢失。**

现在开始修复。我会：
1. 修改 `test/evolution-history.test.ts` 使用临时目录或保存/恢复模式
2. 修改 `test/failure-patterns.test.ts` 同样处理
3. 运行测试验证修复等等，我意识到一个问题。`loadEvolutionHistory()` 函数使用的是硬编码的路径 `DATA_DIR/evolution-history.json`，而不是 TEST_HISTORY_PATH。这意味着测试实际上是在操作真实文件。

我需要修改测试来正确地保存和恢复原始文件，而不是使用临时目录。让我重新修复：Now let me fix the `test/failure-patterns.test.ts` file which also has the same issue:Now let me run the tests to verify the fix works:The evolution-history tests pass! The failures are in `principle-voting.test.ts` which are unrelated to my fix. Let me verify specifically that the evolution-history tests pass:All the tests I modified pass:
- `test/evolution-history.test.ts` - ✅ 8 tests
- `test/state.test.ts` - ✅ 11 tests
- `test/failure-patterns.test.ts` - ✅ 25 tests

The failures in `test/principle-voting.test.ts` are unrelated to my fix (pre-existing issues).

Now let me verify the history file is properly preserved and then commit:
