# Evolution #083 - Cycle #42

**Title:** 优化进化循环超时预警 - evolution-history 显示大量超时失败（20分钟限制）。实现超时预警：接近超时（如18分钟）时提前终止并保存进度，避免完整超时。在 loop.ts 添加时间追踪和预警逻辑。验证：接近超时时优雅终止、进度被保存、pnpm test 通过。方向：经验挖掘 — 优先级：中
**Date:** 2026-03-06T04:12:40.184Z

---

## Evolution Result

我来执行这个任务。首先读取相关代码理解当前实现。让我继续查看 evolution-history 和相关文件，了解超时失败的具体情况：让我检查 evolution-history.json 了解超时失败的具体情况：现在我理解了问题的全貌：
- 从 cycle 16 开始，有大量超时失败（1200000ms = 20分钟）
- 超时失败的 summary 是 `evolution-N timed out after 1200000ms`
- 超时失败后没有任何进度保存

让我查看超时是在哪里设置的：现在我理解了超时机制：
- Worker sessions 有 20 分钟超时 (`DEFAULT_WORKER_TIMEOUT_MS = 20 * 60 * 1000`)
- 超时后直接 reject Error，没有任何进度保存

我需要在 loop.ts 中实现超时预警机制。让我查看更多相关文件来设计实现方案。现在我理解了全部机制。让我实现超时预警功能：现在让我编辑 loop.ts，添加超时预警机制。我会：

1. 在文件开头添加超时预警相关的常量和函数
2. 在 `runEvolutionCycle` 中使用这些函数检查剩余时间
3. 接近超时时优雅处理现在我需要在 `runEvolutionCycle` 函数中添加超时检查。让我读取该函数的具体实现：Let me continue reading the runEvolutionCycle function to understand where to add timeout checks:Now I understand the full structure. Let me add timeout warning checks at key points in the `runEvolutionCycle` function. I'll add:

1. A timeout check before the worker prompt execution (most critical)
2. Special handling for `TimeoutWarningError` in the catch block
3. Logic to save progress when approaching timeout

Let me edit the key sections:Now I need to also handle the case when the actual timeout occurs (from session.ts). Let me read the catch block and add special handling for timeout errors:I'll continue with the implementation. Let me update the catch block to handle timeout errors specially and provide better progress saving:I've added timeout detection and better error handling. Now let me check if the history module needs to be updated to handle the new "timeout" and "timeout-warning" statuses:I see the issue. The `EvolutionRecord` type only allows `"success" | "failed" | "skipped"` for status, but I'm now using `"timeout"` and `"timeout-warning"`. I need to update the type definition:Now I should also update the `calculateEvolutionStats` function to include timeout counts:
